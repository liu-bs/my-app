/**
 * @file page.tsx
 * @description 登录页面入口：渲染登录表单业务组件
 */
import LoginForm from "@/components/business/auth/LoginForm";

/**
 * 登录页面
 * @returns 登录表单组件
 */
export default function LoginPage() {
  return <LoginForm />;
}
